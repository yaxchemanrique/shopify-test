<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {{ 'theme.css' | asset_url | stylesheet_tag }}
    <title>Yaxche Test</title>
    {{ content_for_header }}
  </head>
  <body>
    {% section 'header' %}

    <main id="main-content" role="main">
      {{ content_for_layout }}
    </main>

    {% section 'footer' %}
  </body>
</html>
